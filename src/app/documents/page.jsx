import Documents from "@/clientPages/Documents";
import Batch from "@/clientPages/Documents";

export const metadata = {
  title: "Batch",
};

const page = () => {
  return (
   <Documents/>
  )
}

export default page